---
tags:
  - Rails
---
```
> bundle show {gem name}
/usr/local/bundle/gems/{gem name}-7.1.3
> cd /usr/local/bundle/gems/{gem name}-7.1.3
# vimでソースコードにbinding.pryを挿入
# springがキャッシュしてるのでrails restart
> bin/rails restart
```