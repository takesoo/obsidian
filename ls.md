---
tags:
  - Linux/コマンド
overview: ファイルとディレクトリの参照
---
- ==l==i==s==t
```bash
ls [ディレクトリ名]

ls

ls -a # ドットファイルも表示
.  ..  .bash_history  .bash_logout  .bash_profile  .bashrc

mkdir -p dir1/dir2
ls -R dir1 # dir1以下のすべてを再起的に表示
dir1:
dir2

dir1/dir2:
```