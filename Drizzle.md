## What
### drizzle-orm
### drizzle-kit
- マイグレーションなどDB操作
- スキーマtsにDBのあるべき姿を記載し、`drizzle-kit generate`コマンドでマイグレーションファイルを生成する（[[宣言的スキーマ管理]]）。
	- スキーマts: DBのあるべき最終形
	- ジャーナルファイル：マイグレーションの`tag`（ファイル名）と`when`（生成時刻 ms）を記録した台帳
	- スナップショットファイル：`generate`実行時のスキーマ全体像。
## Why
## How
- `drizzle-kit push`: スキーマ定義と実 DB を比較して、差分 DDL をその場で生成して直接適用する。
- `drizzle-kit generate`: スキーマ定義の変化からマイグレーションファイルを生成する。
	- 生成物：マイグレーションファイル、ジャーナルファイル、スナップショットファイル
- `drizzle-kit migrate`: マイグレーションファイルを適用する
- `drizzle-kit studio`: DB を閲覧編集できる Web GUI を起動する
### `generate`の流れ
1. schema.ts を読み込み、現在のあるべき姿を JSON スナップショットとしてメモリ上に構築する。
2. `meta/` にある前回の `generate` 時のスナップショットと比較する。
3. その差分からマイグレーションファイルを生成し、ジャーナルファイルにエントリを追記する。
### `migrate`の流れ
1. `drizzle.__drizzle_migrations`履歴テーブルから最後に実行したマイグレーションの記録を取得
2. ジャーナルファイルの`when`と比較して、`created_at`より新しいSQLを1つのトランザクションで順に実行する
3. 1件適用するごとに履歴テーブルにinsertする