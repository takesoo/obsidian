---
aliases:
  - declarative schemas
---
## What
- データベースのあるべき最終状態をコードで定義し、ツールが現在との差分を自動計算して適用するデータベース管理手法。
- 定義ファイルを変更した後、ツールが現在のDBとの差分を計算し、SQLを生成、実行する。
### メリット
- 定義ファイルを見れば現在のDBの構造がわかる。全体像が把握しやすい。
- SQLを手動で書かないためミスがない。
- どの環境でも同じ状態に合わせやすい
### デメリット
- 自動処理できない場合がある
- 自動作成されるSQLにミスがある場合がある
## Why
- 命令的スキーマ管理では、マイグレーションファイルのコンフリクト、最新のテーブル構造の把握が困難という問題があった。
- [[Terraform]]や[[Kubernetes]]のように、「あるべき状態を定義する（宣言的）」思想
## How
1. 定義ファイルを書き換える
2. dry-runで差分を確認する
3. 変更を適用する